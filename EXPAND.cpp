#include <iostream>
#include <cstdio>
#include <cmath>
#include <algorithm>
#include <cstring>
using namespace std ;
int main(int argc, char const *argv[])
{
    int test,n,i,a,b,c,d;
    scanf("%d",&test) ;
    while(test)
    {
    	scanf("%d",&n) ;
        for(i=0;i<n;i++)
        {
            scanf("%d%d%d%d",&a,&b,&c,&d) ;
            ss
        }
    	test -- ;
    }
	return 0;
}